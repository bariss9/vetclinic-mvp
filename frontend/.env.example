# No environment variables are required for the frontend in development.
# The API base URL is hardcoded to http://localhost:3000 in src/api.ts.
# If you need to override it for a different environment, add:
# VITE_API_BASE_URL=http://localhost:3000
